// src/BookProgress.cpp

// #include <string>
#include "BookProgress.h"

void BookProgress::saveProgress(std::string filename, int position)
{
    // Save the current position to a file
}

int BookProgress::loadProgress(std::string filename)
{
    // Load the current position from a file
    return 0;
}